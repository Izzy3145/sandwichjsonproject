<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- main text -->
    <style name="main_text" parent="TextAppearance.AppCompat.Body1">
        <item name="android:layout_marginLeft">@dimen/activity_vertical_margin</item>
        <item name="android:layout_marginRight">@dimen/activity_vertical_margin</item>
    </style>

    <!-- subheading -->
    <style name="subheading" parent="main_text">
        <item name="android:textStyle">bold</item>
    </style>

    <!-- sandwich name -->
    <style name="sandwich_name" parent="subheading">
        <item name="android:textSize">24sp</item>
    </style>

</resources>
